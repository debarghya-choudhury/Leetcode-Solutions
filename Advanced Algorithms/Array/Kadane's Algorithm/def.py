# Kadane's Algo

# if all the elements in the array are negative, we can just return the largest number in the array O(n)
# because if we add negative numbers it will just become more negative

# same with positive but in reverse